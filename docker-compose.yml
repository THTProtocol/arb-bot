services:
  arb-bot:
    build: .
    volumes:
      - ./data:/app/data
    ports:
      - "9090:9090"
    environment:
      - RUST_LOG=info
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9091:9090"
