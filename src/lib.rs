// This is the workspace root lib.rs, currently placeholder
// The binary main.rs is in crates/arb-bin/src/main.rs

#[cfg(test)]
mod tests {
    #[test]
    fn workspace_sanity() {
        assert_eq!(2 + 2, 4);
    }
}
